name: Daily LLMs.txt Regeneration

on:
  schedule:
    - cron: '0 6 * * *' # Daily at 6:00 AM UTC
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  regenerate:
    name: Regenerate llms.txt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate llms.txt files
        run: node scripts/generate-llms.mjs

      - name: Check for changes
        id: diff
        run: |
          # --porcelain (not diff) so untracked docs-raw mirrors for new pages
          # are picked up, not just edits to already-committed ones.
          git status --porcelain -- static/llms.txt static/llms-full.txt static/docs-raw
          echo "changed=$(git status --porcelain -- static/llms.txt static/llms-full.txt static/docs-raw | wc -l | tr -d ' ')" >> "$GITHUB_OUTPUT"

      - name: Commit and push
        if: steps.diff.outputs.changed != '0'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add static/llms.txt static/llms-full.txt static/docs-raw
          git commit -m "chore: auto-regenerate llms.txt and docs-raw mirrors"
          git push
