---
slug: api
sidebar_position: 0
title: API Overview
description: Overview of all SealMetrics API sections including authentication, endpoints, event tracking and implementation guides.
---
# API Overview

Welcome to the **SealMetrics API Reference**.  
Here you’ll find everything you need to integrate modern, privacy-first analytics into your product.

---

import Link from '@docusaurus/Link';

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))',
    gap: '1.5rem',
    marginTop: '2rem',
  }}
>

  {/* Introduction */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/introduction" style={{ textDecoration: 'none', color: '#00825C' }}>
        Introduction
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Start here to understand how the SealMetrics API works and how to get access.
    </p>
  </div>

  {/* Authentication */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/authentication" style={{ textDecoration: 'none', color: '#00825C' }}>
        Authentication
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Learn how to authenticate using API keys and ensure secure data access.
    </p>
  </div>

  {/* Core API Endpoints */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/core-api-endpoints" style={{ textDecoration: 'none', color: '#00825C' }}>
        Core API Endpoints
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Query aggregated data, traffic, conversions, revenue, and more.
    </p>
  </div>

  {/* Event Tracking */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/event-tracking" style={{ textDecoration: 'none', color: '#00825C' }}>
        Event Tracking
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Learn how to send events, conversions, micro-conversions, and custom properties.
    </p>
  </div>

  {/* Implementation Guide */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/implementation-guide" style={{ textDecoration: 'none', color: '#00825C' }}>
        Implementation Guide
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Step-by-step examples for building API-based reporting and integrations.
    </p>
  </div>

  {/* Appendix */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/appendix" style={{ textDecoration: 'none', color: '#00825C' }}>
        Appendix
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Reference tables, constants, data definitions, and additional technical notes.
    </p>
  </div>

</div>

---

If you need help extending the API or want early access to new endpoints, let us know → hello@sealmetrics.com