---
slug: /api
sidebar_position: 0
title: API Overview
description: Overview of all Sealmetrics API sections including authentication, endpoints, event tracking and implementation guides.
---
# API Overview

Welcome to the **Sealmetrics API Reference**.
Here you'll find everything you need to integrate modern, privacy-first analytics into your product.

import Link from '@docusaurus/Link';

<div style={{
  background: 'linear-gradient(135deg, #00825C 0%, #005a3c 100%)',
  borderRadius: '12px',
  padding: '1.5rem 2rem',
  marginBottom: '2rem',
  color: 'white',
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  flexWrap: 'wrap',
  gap: '1rem'
}}>
  <div>
    <h3 style={{ color: 'white', margin: 0 }}>OpenAPI Specification</h3>
    <p style={{ margin: '0.5rem 0 0', opacity: 0.9 }}>
      Download our complete API spec for Postman, Swagger, or SDK generation.
    </p>
  </div>
  <a
    href="/openapi.yaml"
    download="sealmetrics-openapi.yaml"
    style={{
      display: 'inline-flex',
      alignItems: 'center',
      gap: '0.5rem',
      background: 'white',
      color: '#00825C',
      padding: '0.75rem 1.5rem',
      borderRadius: '8px',
      fontWeight: '600',
      textDecoration: 'none',
      whiteSpace: 'nowrap'
    }}
  >
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
      <polyline points="7 10 12 15 17 10"/>
      <line x1="12" y1="15" x2="12" y2="3"/>
    </svg>
    Download YAML
  </a>
</div>

---

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))',
    gap: '1.5rem',
    marginTop: '2rem',
  }}
>

  {/* OpenAPI Reference */}
  <div
    style={{
      border: '2px solid #00825C',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,130,92,0.15)',
      transition: 'transform 0.2s ease',
      background: 'linear-gradient(135deg, rgba(0,130,92,0.05) 0%, rgba(0,130,92,0.02) 100%)',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/openapi-reference" style={{ textDecoration: 'none', color: '#00825C' }}>
        OpenAPI Reference
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Complete API specification with all endpoints, schemas, and examples. Download for Postman or SDK generation.
    </p>
  </div>

  {/* Quick Start */}
  <div
    style={{
      border: '2px solid #00825C',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,130,92,0.15)',
      transition: 'transform 0.2s ease',
      background: 'linear-gradient(135deg, rgba(0,130,92,0.05) 0%, rgba(0,130,92,0.02) 100%)',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/quick-start" style={{ textDecoration: 'none', color: '#00825C' }}>
        Quick Start
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Get your first API request working in 5 minutes. Code examples in Python, JavaScript, and more.
    </p>
  </div>

  {/* Introduction */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/introduction" style={{ textDecoration: 'none', color: '#00825C' }}>
        Introduction
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Start here to understand how the Sealmetrics API works and how to get access.
    </p>
  </div>

  {/* Authentication */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/authentication" style={{ textDecoration: 'none', color: '#00825C' }}>
        Authentication
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Learn how to authenticate using API keys and ensure secure data access.
    </p>
  </div>

  {/* Core API Endpoints */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/core-api-endpoints" style={{ textDecoration: 'none', color: '#00825C' }}>
        Core API Endpoints
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Query aggregated data, traffic, conversions, revenue, and more.
    </p>
  </div>

  {/* Event Tracking */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/event-tracking" style={{ textDecoration: 'none', color: '#00825C' }}>
        Event Tracking
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Learn how to send events, conversions, micro-conversions, and custom properties.
    </p>
  </div>

  {/* Implementation Guide */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/implementation-guide" style={{ textDecoration: 'none', color: '#00825C' }}>
        Implementation Guide
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Step-by-step examples for building API-based reporting and integrations.
    </p>
  </div>

  {/* Appendix */}
  <div
    style={{
      border: '1px solid #e0e0e0',
      borderRadius: '12px',
      padding: '1.5rem',
      boxShadow: '0 2px 8px rgba(0,0,0,0.05)',
      transition: 'transform 0.2s ease',
    }}
    onMouseEnter={(e) => (e.currentTarget.style.transform = 'translateY(-4px)')}
    onMouseLeave={(e) => (e.currentTarget.style.transform = 'translateY(0)')}
  >
    <h3 style={{ color: '#00825C' }}>
      <Link to="/api/appendix" style={{ textDecoration: 'none', color: '#00825C' }}>
        Appendix
      </Link>
    </h3>
    <p style={{ fontSize: '0.9rem', lineHeight: '1.5rem' }}>
      Reference tables, constants, data definitions, and additional technical notes.
    </p>
  </div>

</div>

---

If you need help extending the API or want early access to new endpoints, let us know → hello@sealmetrics.com