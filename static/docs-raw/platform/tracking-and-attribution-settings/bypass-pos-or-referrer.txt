# Bypass POS or Referrer

URL: https://docs.sealmetrics.com/platform/tracking-and-attribution-settings/bypass-pos-or-referrer

# Bypass POS or Referrer

When a user completes a purchase through a **POS (Point of Sale)** or external payment page (Stripe, PayPal, Redsys…), they temporarily leave your site.  
When they return to your **thank-you page**, Sealmetrics may detect this as a new session and attribute the referrer incorrectly.

This causes conversions to appear as coming from:
❌ *payment* instead of  
✔️ the original source (Google Ads, Facebook, Email, etc.)

---

## How Bypass POS / Referrer Works

You can configure Sealmetrics to **ignore specific referrers**, ensuring that once users return from a payment domain, the system keeps the original traffic source instead of overwriting it.

---

## How to Set It Up

1. Open the settings menu in the top toolbar and click **Accounts**
2. In the accounts table, find your account and click the **POS Referral Bypass** action in its row
3. On the **Referral Exclusion List** screen, type a domain to exclude (e.g. a payment provider) in the **Domain** field
4. Click **Add referral** to add another domain field if needed
5. Click **Update** to save

To remove a domain, click **Delete** next to it.

---

## Example Screenshots

Open the settings menu, choose **Accounts**, then click **POS Referral Bypass** on the account's row:

![Accounts table with the POS Referral Bypass action](/img/platform/bypass-pos-referrer-1.png)

The Referral Exclusion List, where you add the domains to exclude:

![Referral Exclusion List screen](/img/platform/bypass-pos-referrer-2.png)

---

## Why This Matters

This avoids:
- Incorrect attribution to *payment*
- Lost ROAS visibility  
- Broken marketing funnels  

You keep the **real traffic source** intact.

---